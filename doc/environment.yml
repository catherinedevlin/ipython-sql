name: jupysql-doc

channels:
  - conda-forge


dependencies:
  - python=3.10
  - matplotlib
  - pandas
  - pip
  - pip:
    - jupyter-book
    # duckdb example
    - duckdb
    - duckdb-engine
    # plot example
    - memory-profiler
    - psycopg2-binary
    - pyarrow
    - ..